services:
  postgres:
    image: postgres:16
    container_name: skillnode-postgres
    environment:
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
      POSTGRES_DB: skillnode
    ports:
      - "5432:5432"
    volumes:
      - skillnode_pg:/var/lib/postgresql/data

volumes:
  skillnode_pg:

